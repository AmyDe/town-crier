<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runtime-requested by the onboarding wizard's notification-permission
         step on API 33+ (tc-7ttz); a no-op declaration below 33, where
         notification delivery needs no runtime grant at all. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="uk.towncrierapp.app.TownCrierApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- Fully-qualified deliberately: the Kotlin source package
             (uk.towncrierapp.app, matching the module-naming convention) is
             not the manifest namespace (uk.towncrierapp.mobile, the branding
             applicationId). A relative ".MainActivity" would resolve against
             the namespace and silently point at a class that doesn't exist. -->
        <activity
            android:name="uk.towncrierapp.app.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The GDPR data-export share sheet's content:// bridge (tc-4jjw / #778).
             authorities uses ${applicationId} so the dev/prod/local flavors
             (different applicationIdSuffix) each get their own non-clashing
             authority. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
